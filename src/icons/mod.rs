use crate::utils::import;

import!(
    twitter, ts, sun, sqlite, react, rust, paypal, python, node, next, newspaper, dotnet, mysql,
    moon, code, csharp, discord, flask, github, home, instagram, js, mail
);
